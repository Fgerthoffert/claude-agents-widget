{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    /* Pulls hooks/*.mjs into the program so ESLint's project service can parse it.
       `checkJs` stays off: the hook script is plain ESM, verified by its integration test. */
    "allowJs": true,
    "isolatedModules": true,
    /* `node` for vite.config.ts / vitest.config.ts, which this project also type-checks. */
    "types": ["node"],
    "verbatimModuleSyntax": true,
    "noEmit": true,
    "jsx": "react-jsx",

    /* Strictness — see docs/adr/0004-functional-typescript-style.md */
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "noImplicitOverride": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src", "hooks", "scripts", "vite.config.ts", "vitest.config.ts"]
}
